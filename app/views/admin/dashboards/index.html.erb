<h1>All users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
      <%= link_to 'Delete', admin_user_path(user), method: :delete, date: { confirm: 'Are you sure you want to delete?' } %>
    </li>
  <% end %>
</ul>